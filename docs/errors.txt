Error messages

*LET
*Bad expression

*GOTO
*Bad line number

*ON GOTO/Gosub
*Bad value (index out of range)
*Bad line number 

IF
Note: + is used for OR and * for And
Also, expressions that evaluate to 0 are true, anything else is false

For/Next
Note: if initial value > limit, the loop is not run
Bad value (step is 0)
For next error (mismatched for/next)
Can't do that (bad nested loops)


